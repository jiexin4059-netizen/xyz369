# 公司账目管理系统

这是一个给编程新手也能直接使用的静态网页版本公司账目管理系统。

## 如何使用

1. 直接用浏览器打开 `index.html`。
2. 输入员工号登入，例如 `EMP001`。每个员工号会看到自己的账目记录。
3. 选择银行，然后填写：
   - 日期
   - Ref 单据编号
   - In（收）
   - Out（出）
4. 点击“添加记录”保存。
5. 可以用“查看银行”筛选某一家银行或查看全部银行。

数据会保存在当前浏览器的 localStorage 中，不需要数据库。
